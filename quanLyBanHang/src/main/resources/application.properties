# Neon Database (PostgreSQL) Configuration
spring.datasource.url=jdbc:postgresql://ep-silent-boat-a14wejrc-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_LuahzTi3C2Ac
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration for PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Database Initialization (will run schema.sql and data.sql)
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql
spring.sql.init.continue-on-error=true

# Server configuration
server.port=8080

# Application name
spring.application.name=quan-ly-ban-hang

# Logging configuration
logging.level.com.example=INFO
logging.level.controller=DEBUG
logging.level.org.hibernate.SQL=DEBUG